package com.example.casasapp.ui.theme

import androidx.compose.ui.graphics.Color

// ===========================================
// PALETA DE COLORES "CLEAN & BOLD"
// Alto contraste y máxima legibilidad
// ===========================================

// Primary - Azul Eléctrico profesional
val Primary = Color(0xFF0055FF)
val PrimaryLight = Color(0xFF4D8AFF)
val PrimaryDark = Color(0xFF0044CC)
val OnPrimary = Color.White

// Secondary - Azul más suave para acentos secundarios
val Secondary = Color(0xFF3D7FE8)
val SecondaryLight = Color(0xFF6FA3FF)
val SecondaryDark = Color(0xFF2563EB)
val OnSecondary = Color.White

// Tertiary - Gris azulado para elementos terciarios
val Tertiary = Color(0xFF64748B)
val TertiaryLight = Color(0xFF94A3B8)
val TertiaryDark = Color(0xFF475569)
val OnTertiary = Color.White

// ============================================
// BACKGROUNDS Y SURFACES - Light Mode Only
// Para máximo contraste y legibilidad
// ============================================

// Background - Gris perla muy suave (fondo de pantalla)
val Background = Color(0xFFF2F4F7)
val OnBackground = Color(0xFF1A1C1E)

// Surface - Blanco puro (para tarjetas)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF8FAFC)

// OnSurface - Negro suave para textos principales (Alta legibilidad)
val OnSurface = Color(0xFF1A1C1E)

// OnSurfaceVariant - Gris oscuro para subtítulos
val OnSurfaceVariant = Color(0xFF40454B)

// ============================================
// OUTLINE Y DIVISORES
// ============================================
val Outline = Color(0xFFD1D5DB)
val OutlineVariant = Color(0xFFE5E7EB)

// ============================================
// ESTADOS DE ERROR
// ============================================
val Error = Color(0xFFDC2626)
val ErrorLight = Color(0xFFFEE2E2)
val OnError = Color.White
val ErrorContainer = Color(0xFFFECACA)
val OnErrorContainer = Color(0xFF991B1B)

// ============================================
// COLORES PARA TIPOS DE PROPIEDAD
// ============================================
val VentaColor = Color(0xFF16A34A)      // Verde vibrante para Venta
val AlquilerColor = Color(0xFFF97316)   // Naranja cálido para Alquiler

// ============================================
// GRADIENTES (para uso ocasional)
// ============================================
val GradientStart = Primary
val GradientEnd = PrimaryLight

// ============================================
// SOMBRAS
// ============================================
val ShadowColor = Color(0x1A000000)